import React from "react";

type Props = {};

interface Education {
  School: string;
  Degree: string;
  FieldOfStudy: string;
  StartDate: string;
  EndDate: string;
}

const Education = (props: Props) => {
  return <div>Education</div>;
};

export default Education;
